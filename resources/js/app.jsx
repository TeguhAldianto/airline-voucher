import React from "react";
import ReactDOM from "react-dom/client";
import VoucherApp from "./VoucherApp";

ReactDOM.createRoot(document.getElementById("app")).render(
    <React.StrictMode>
        <VoucherApp />
    </React.StrictMode>
);
